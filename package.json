{
  "name": "my-awesome-game",
  "version": "1.0.0",
  "description": "my-awesome-game project with server and UI",
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:ui": "cd ui && npm run dev",
    "install:all": "npm install && cd server && npm install && cd ../ui && npm install",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:ui --host\""
  },
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}
